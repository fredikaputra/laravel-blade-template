<?php

declare(strict_types=1);

return [

    /*
    |--------------------------------------------------------------------------
    | Frontend Application URL
    |--------------------------------------------------------------------------
    |
    | This URL is used by the application to properly generate URLs when linking
    | to your frontend application. You should set this to the root of
    | the frontend so that it's available within emails and notifications.
    |
    */

    'frontend_url' => env('FRONTEND_URL', 'http://localhost:5173'),

];
